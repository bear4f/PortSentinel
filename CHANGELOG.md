# Changelog

## 1.4.0 - 2026-08-29

### 域名来源

- 规则和 IP Group 的来源现在可以直接填写域名，与 IP/CIDR 混在一行用逗号分隔。
- 配置中保存域名本身而不是解析出的 IP：每次 `apply`（含 `--dry-run`）前重新解析，
  域名指向新地址后重新应用即可跟随，适合 IP 会变的中转机。
- 输入时立即解析并回显结果，解析不到的域名当场拒绝，不会写进配置。
- 解析结果缓存在 `/etc/portsentinel/resolved.json`：解析失败时沿用上次结果并告警，
  避免 DNS 故障把白名单缩空；从未解析成功过的域名会明确告警且不进入白名单，
  交互式执行需要确认后才继续。
- 配置中不再引用的域名会自动从缓存清理。
- 新增 `portsentinel resolve` 命令与 IP Group 菜单 `[7] 刷新域名解析`。
- 域名解析结果同样参与 SSH 防锁死判断。
- 新增 `PORTSENTINEL_RESOLVER`（自定义解析程序）与 `PORTSENTINEL_DNS_TIMEOUT`
  （默认 5 秒）两个环境变量。
- 配置结构向后兼容：`domains` 字段可选，旧配置无需迁移。

## 1.3.1 - 2026-08-29

修复四个会让人误判防火墙真实状态的问题。

- 配置中删除全部受保护端口后，Apply 不再直接返回"无需应用"，而是移除 `INPUT`
  跳转和私有链，把防火墙收敛到与配置一致的状态。交互式执行会先确认，
  `--dry-run` 只说明将要移除的内容。
- 面板中的事务操作跑在 `( )` 子 shell 里，而 bash 会重置子 shell 的 trap：
  此前 Apply 途中的 `SIGINT` 会被吞掉、`SIGTERM` 会直接杀死进程且不回滚，
  留下 IPv4 已切换、IPv6 未切换的半应用状态。现在 `apply` 与 `restore`
  会各自重新安装信号处理，两条路径的回滚保证一致。
- `BACKUP_KEEP` 下限提高到 2，轮转时显式跳过本次事务备份，避免刚创建的
  回滚依据被立刻删除（`BACKUP_KEEP=0` 时必然发生）。
- 失败文案改为如实描述：回滚成功报告所用备份编号，回滚失败明确要求手动恢复，
  不再无条件宣称"IPv4 和 IPv6 均已回滚"。
- `reset` 和 `restore` 之后清除应用标记，面板不会再在链已被移除或防火墙来自
  旧备份时显示"已与防火墙同步"。
- 新增 `test-empty-ruleset.sh`、`test-signal-rollback.sh`、`test-backup-keep.sh`
  三个回归测试，覆盖上述场景。

## 1.3.0 - 2026-08-29

### 交互面板

- 重做 SSH 交互界面：菜单按“常用操作 / 策略管理 / 系统维护”分组，每项只占一行，整屏行数减少约一半。
- 状态区改为显示真实链状态（规则已生效 / 未生效 / 已被系统禁用），不再只判断后端命令是否存在。
- 新增“配置状态”提示，配置改动后才显示待应用，应用成功后自动转为已同步。
- 修正标题框和分隔线：按终端显示宽度对齐，中日韩字符按两列计算，窄终端自动隐藏说明列。
- 主菜单去掉与子菜单重复的入口，`[1] 添加端口保护` 与 `[2] 应用配置` 固定为最常用操作。
- 备份与恢复改为独立子菜单，新增 `[h] 帮助`。

### 交互逻辑

- 任何输入步骤都可以输入 `q` 放弃当前操作并返回菜单。
- 协议、默认策略、编辑字段、白名单来源、SSH 防锁死处理方式一律改为编号选择，不再手动输入 `tcp`、`drop` 等关键字。
- 删除规则、删除 Group、重置防火墙改用 `[y/N]` 确认，不再要求逐字输入大写口令。
- 规则和 Group 的地址增删改为批量输入并自动识别地址族，取消单独的地址族提问，并在删除前列出现有地址。
- 输入错误统一在当前步骤重试；重命名、解绑、删除等操作不再因一次输错而中断。
- 恢复备份支持按序号选择，并在恢复前校验备份组确实存在。

### 修复

- 修复安全备份（`*-pre-restore`）因时间戳格式校验而无法恢复的问题。
- 修复同一秒内多次恢复时安全备份互相覆盖的问题。
- 主机缺少 `ip6tables-save` / `ip6tables-restore` 时不再直接失败，改为提示并继续处理 IPv4。
- 备份排序固定使用 `C` 排序规则，避免不同 locale 下轮转顺序异常。

## 1.0.0 - 2026-08-29

- Initial dual-stack TCP/UDP port whitelist manager.
- Transactional IPv4/IPv6 apply with paired rollback backups.
- Interactive menu, reusable IP groups, systemd persistence, and safe update command.
