# Changelog

## 1.3.0 - 2026-08-29

### 交互面板

- 重做 SSH 交互界面：菜单按“常用操作 / 策略管理 / 系统维护”分组，每项只占一行，整屏行数减少约一半。
- 状态区改为显示真实链状态（规则已生效 / 未生效 / 已被系统禁用），不再只判断后端命令是否存在。
- 新增“配置状态”提示，配置改动后才显示待应用，应用成功后自动转为已同步。
- 修正标题框和分隔线：按终端显示宽度对齐，中日韩字符按两列计算，窄终端自动隐藏说明列。
- 主菜单去掉与子菜单重复的入口，`[1] 添加端口保护` 与 `[2] 应用配置` 固定为最常用操作。
- 备份与恢复改为独立子菜单，新增 `[h] 帮助`。

### 交互逻辑

- 任何输入步骤都可以输入 `q` 放弃当前操作并返回菜单。
- 协议、默认策略、编辑字段、白名单来源、SSH 防锁死处理方式一律改为编号选择，不再手动输入 `tcp`、`drop` 等关键字。
- 删除规则、删除 Group、重置防火墙改用 `[y/N]` 确认，不再要求逐字输入大写口令。
- 规则和 Group 的地址增删改为批量输入并自动识别地址族，取消单独的地址族提问，并在删除前列出现有地址。
- 输入错误统一在当前步骤重试；重命名、解绑、删除等操作不再因一次输错而中断。
- 恢复备份支持按序号选择，并在恢复前校验备份组确实存在。

### 修复

- 修复安全备份（`*-pre-restore`）因时间戳格式校验而无法恢复的问题。
- 修复同一秒内多次恢复时安全备份互相覆盖的问题。
- 主机缺少 `ip6tables-save` / `ip6tables-restore` 时不再直接失败，改为提示并继续处理 IPv4。
- 备份排序固定使用 `C` 排序规则，避免不同 locale 下轮转顺序异常。

## 1.0.0 - 2026-08-29

- Initial dual-stack TCP/UDP port whitelist manager.
- Transactional IPv4/IPv6 apply with paired rollback backups.
- Interactive menu, reusable IP groups, systemd persistence, and safe update command.
